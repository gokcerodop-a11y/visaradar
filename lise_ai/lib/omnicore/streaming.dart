// streaming.dart
// Barrel for the future `omnicore_streaming` package.
//
// Pacing-aware streaming wrapper. Phase 5 generalizes PacingProfile
// and SpeechTagParser so the wrapper is provider-agnostic.

export '../services/streaming_teacher_session.dart';
